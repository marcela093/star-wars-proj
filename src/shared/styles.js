import styled from 'styled-components'

export const Button = styled.button`
  border: none;
  margin: 8px;
  width: 40%;
  background: rgba(22, 22, 22, 0.902);
  border-radius: 100%;
  outline: none;

  &:hover {
    cursor: pointer;
  }
`
